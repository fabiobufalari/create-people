# ===================
# ⚙️ Server Port
# ===================
server:
  port: 8085 # Define a porta do servidor para 8085

# ======================
# 🔧 Configuração geral
# ======================
authentication:
  service:
    url: http://localhost:8083 # Define a porta do Authenticador para 8083

spring:
  application:
    name: create-people
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # ✅ Cria/tenta atualizar tabelas automaticamente
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher # compatibilidade com Feign
  jackson:
    serialization:
      indent_output: true
  devtools:
    restart:
      enabled: true

# ============================
# 🔐 JWT (Token Secret Key)
# ============================

jwt:
  secret: "404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970!"

# =====================
# 🌎 Mensagens i18n
# =====================

spring.messages:
  basename: messages/messages
  encoding: UTF-8
  fallback-to-system-locale: false
  cache-duration: 10

# ============================
# ☁️ OpenFeign (Authentication)
# ============================

feign:
  client:
    config:
      default:
        loggerLevel: basic
  okhttp:
    enabled: true
  httpclient:
    enabled: false

# ===================
# 🔄 Cloud Discovery
# ===================

spring.cloud:
  openfeign:
    circuitbreaker:
      enabled: true
  loadbalancer:
    ribbon:
      enabled: false
# ============================
# 🗺️ Geocoding Configuration
# ============================
geocoding:
  api:
    url: "https://maps.googleapis.com/maps/api/geocode/json"
    key: "AIzaSyA3jbxRsV1FSjXQYD3g5rYjvoKt9yI4ZkQ"
    timeout: 5000
    retries: 3
    initialInterval: 1000 # ✅
    maxInterval: 5000     # ✅
    maxAttempts: 3        # ✅

logging:
  level:
    org.springframework: INFO
    com.bufalari: DEBUG